# Minimal Flask Demo + GitHub Actions CI

## Run locally

```bash
python -m pip install -r requirements.txt
python app/app.py
```

Then open:

- `http://127.0.0.1:5000/health`
- `http://127.0.0.1:5000/hello`

Run tests:

```bash
pytest -q
```

